import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    keys: {
        display: "flex",
        flexWrap: "wrap",
        flexDirection: 'row'
    }
})

export default styles