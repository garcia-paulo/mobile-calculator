import { Dimensions, StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        padding: 20,
        alignItems: 'flex-end'
    },

    text: {
        fontSize: 60
    }
})

export default styles;