import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    root: {
        display: 'flex'
    },

    item: {
        alignItems: 'flex-end',
        paddingRight: 17,
        padding: 3,
    },

    text: {
        color: '#666'
    }
})

export default styles;